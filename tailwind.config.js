const defaultTheme = require('tailwindcss/defaultTheme')

/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', //Remove this once we migrate all pages to dark mode
  content: [],
  prefix: 'tw-',
}

